.. highlight:: shell

============
Installation
============


Stable release
--------------

To install pyadi-jif, run this command in your terminal:

.. code-block:: console

    $ pip install adijif

This is the preferred method to install pyadi-jif, as it will always install the most recent stable release.

If you don't have `pip`_ installed, this `Python installation guide`_ can guide
you through the process.

.. _pip: https://pip.pypa.io
.. _Python installation guide: http://docs.python-guide.org/en/latest/starting/installation/


From sources
------------

The sources for pyadi-jif can be downloaded from the `Github repo`_.

You can either clone the public repository:

.. code-block:: console

    $ git clone git://github.com/tfcollins/adijif

Or download the `tarball`_:

.. code-block:: console

    $ curl -OJL https://github.com/tfcollins/adijif/tarball/master

Once you have a copy of the source, you can install it with:

.. code-block:: console

    $ python setup.py install


.. _Github repo: https://github.com/tfcollins/adijif
.. _tarball: https://github.com/tfcollins/adijif/tarball/master
